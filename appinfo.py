"""Identidade do app — fonte única para versão e URL do servidor.

Importado por license_client e auto_update; os .spec de build usam a mesma
versão via LicenseClient.APP_VERSION.
"""

APP_VERSION = "1.69"
SERVER_URL = "https://nicolasapp.duckdns.org"
